/**
 * ClassRoom Representation.
 */
export default class ClassRoom {
  /**
   * Creates  @see {@link ClassRoom}.
   * @param {Number} maxStudentsSize - max number of student
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
